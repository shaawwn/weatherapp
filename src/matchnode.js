

class Node {
    // a linked list node for city matches

    constructor(current, previous) {
        this.current = current // Current cities match
        this.previous = previous // previous cities match node
    }
}

export default Node