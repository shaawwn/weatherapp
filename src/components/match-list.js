import React, {useState, useEffect} from 'react';

function MatchList(props) {
    const [parent, setParent] = useState(props.prev)
    const [cityList, setCityList] = useState(props.list)
}

export default MatchList